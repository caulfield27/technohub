import { makeStyles } from "@fluentui/react";
import { tokens } from "@fluentui/react-components";

export const usePartyInfostyles = makeStyles({
    button_footer: {
        // width: '200px'
        display: 'flex',
        justifyContent: 'end'
    }
});
