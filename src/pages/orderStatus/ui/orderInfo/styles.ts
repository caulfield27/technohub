import { makeStyles } from "@fluentui/react";

export const useOrderinfostyles = makeStyles({
    button_footer: {
        // width: '200px'
        display: 'flex',
        justifyContent: 'end'
    }
});
